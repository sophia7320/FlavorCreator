# 创味机 Flavor Creator - API 接口文档

> 微信小程序版本

## 一、接口规范

### 1.1 基础信息
- **协议**: HTTP/HTTPS
- **数据格式**: `application/json`
- **字符编码**: UTF-8
- **时间格式**: ISO 8601 (`YYYY-MM-DDTHH:mm:ssZ`)

### 1.2 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 1.3 错误码
| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录/Token 过期 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 1001 | 微信 code 无效 |
| 1002 | 微信接口调用失败 |
| 1003 | 手机号获取失败 |

---

## 二、微信登录模块

### 2.1 微信一键登录（核心接口）
```
POST /api/auth/login-wx
```

**请求参数**:
```json
{
  "code": "微信登录码，通过 wx.login() 获取",
  "encryptedData": "用户数据签名（获取手机号时需要）",
  "iv": "用户数据签名向量（获取手机号时需要）",
  "userInfo": {
    "nickName": "用户昵称",
    "avatarUrl": "用户头像",
    "gender": 1
  }
}
```

**响应**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "refresh_xxxx...",
    "expiresIn": 7200,
    "needBindPhone": false,
    "user": {
      "id": 10001,
      "nickname": "用户昵称",
      "avatar": "https://...",
      "phone": "138****0000",
      "isNewUser": false
    }
  }
}
```

**后端处理逻辑**:
1. 使用 `code` 调用微信 `auth.code2Session` 接口获取 `openid` 和 `session_key`
2. 判断用户是否已注册（根据 openid 查询）
3. 已注册：生成 token 返回用户信息
4. 未注册：自动创建用户并返回 `isNewUser: true`

---

### 2.2 获取并绑定微信手机号
```
POST /api/auth/phone-wx
```

**请求参数**:

```json
{
  "code": "wx.getPhoneNumber() 返回的 code"
}
```

**响应**:
```json
{
  "code": 200,
  "message": "绑定成功",
  "data": {
    "phoneNumber": "13800138000",
    "purePhoneNumber": "13800138000",
    "countryCode": "86"
  }
}
```

**说明**: 
- 用户点击手机号获取按钮后，前端调用微信 API 获取 code 传给后端
- 后端调用微信接口解密获取手机号
- 该接口可在登录时或登录后调用

---

### 2.3 刷新 Token
```
POST /api/v1/auth/refresh
```

**请求参数**:
```json
{
  "refreshToken": "刷新令牌"
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "token": "新的访问令牌",
    "refreshToken": "新的刷新令牌",
    "expiresIn": 7200
  }
    "message": "success"
}
```

---

### 2.4 微信登录 - 后端与微信接口交互

后端需要调用微信官方接口：

| 接口 | 说明 |
|------|------|
| `https://api.weixin.qq.com/sns/jscode2session` | code2Session 获取 openid 和 session_key |
| `https://api.weixin.qq.com/wxa/business/getuserphonenumber` | 获取用户手机号 |

**code2Session 请求示例**:
```
GET https://api.weixin.qq.com/sns/jscode2session?appid=APPID&secret=SECRET&js_code=CODE&grant_type=authorization_code
```

**响应**:
```json
{
  "openid": "oXXXX_openid",
  "session_key": "xxxx_session_key",
  "unionid": "oXXXX_unionid"
}
```

**获取手机号请求示例**:
```
POST https://api.weixin.qq.com/wxa/business/getuserphonenumber?access_token=ACCESS_TOKEN
```

**请求体**:
```json
{
  "code": "微信返回的 code"
}
```

**响应**:
```json
{
    "code" : 200 ,
  	"phone_info": {
    	"phoneNumber": "13800138000",
    	"purePhoneNumber": "13800138000",
    	"countryCode": "86"
  	},
    "message": "success"
}
```

---

### 2.5 退出登录

（前端将token置空）

```
POST /api/auth/logout
```

**Headers**: `Authorization: Bearer <token>`

**响应**:

```json
{
  	"code": 200,
    "data" : null
  	"message": "退出成功"
}
```

---

## 三、用户信息模块

### 3.1 获取用户信息
```
GET /api/user/info
```

**Headers**: `Authorization: Bearer <token>`

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10001,
    "openid": "oXXXX_openid",
    "nickname": "用户昵称",
    "avatar": "https://...",
    "background": "https://...",
    "signature": "个性签名",
    "gender": 1,
    "phone": "138****0000",
    "preferences": {
      "taste": ["清淡", "少油"],
      "dietary": ["低卡"],
      "cookTime": 30,
      "difficulty": "简单"
    },
    "stats": {
      "followingCount": 10,
      "followerCount": 50,
      "likeCount": 200,
      "collectionCount": 30
    }
  },
    "message": "success"
}
```

---

### 3.2 更新用户信息
```
POST /api/user/info
```

**请求参数**:

```json
{
  "nickname": "新昵称",
  "signature": "新签名",
  "background": "https://...",
  "gender": 1,
  "preferences": {
    "taste": ["清淡", "少盐"],
    "dietary": ["低卡", "高蛋白"],
    "cookTime": 20,
    "difficulty": "简单"
  }
}
```

**响应**:

```json
{
  "code": 200,
  "message": "更新成功"
}
```

---

### 3.3 上传头像
```
POST /api/user/avatar
```

**请求类型**: `multipart/form-data`

**请求参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| file | file | 头像图片文件 |

**响应**:
```json
{
  "code": 200,
  "data": {
    "avatarUrl": "https://..."
  }
    "message": "success"
}
```

---

### 3.4 上传背景图 (参数和响应同头像)
```
POST /api/v1/user/background
```

**请求类型**: `multipart/form-data`

---

## 四、菜谱模块

### 4.1 食材输入
```
POST /api/recipe/apply
```

**Headers**: `Authorization: Bearer <token>`

**请求参数**:

```json
{
  "ingredients": [
    {"name": "鸡蛋", "quantity": 3, "unit": "个"},
    {"name": "西红柿", "quantity": 2, "unit": "个"}
  ],
  "preferences": {
    "taste": ["清淡"],
    "cookTime": 30,
    "difficulty": "简单"
  }
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "matchDegree": 92,
    "recipes": [
      {
        "id": 1001,
        "name": "西红柿炒鸡蛋",
        "cover": "https://...",
        "matchDegree": 95,
        "cookTime": 15,
        "difficulty": "简单",
        "calories": 320,
        "tags": ["家常菜", "快手菜"]
      }
    ],
    "needAiGenerate": false
  }
    "message": "success"
}
```

---

### 4.2 AI 生成菜谱
```
POST /api/recipe/ai-generate
```

**请求参数**:
```json
{
  "ingredients": [
    {"name": "鸡蛋", "quantity": 3, "unit": "个"},
    {"name": "青椒", "quantity": 1, "unit": "个"}
  ],
  "preferences": {
    "taste": ["清淡", "少油"],
     "dietary": ["低卡"],
     "cookTime": 30,
     "difficulty": "简单"
  }
}
```

**响应**:

```json
{
  "code": 200,
  "data": {
    "recipe": {
      "name": "青椒炒鸡蛋",
      "ingredients": [
        {"name": "鸡蛋", "quantity": 3, "unit": "个"},
        {"name": "青椒", "quantity": 1, "unit": "个"},
        {"name": "盐", "quantity": 3, "unit": "克"}
      ],
      "steps": [
        {"order": 1, "description": "鸡蛋打散，加少许盐搅拌均匀"},
        {"order": 2, "description": "青椒洗净切块"},
        {"order": 3, "description": "热锅凉油，倒入蛋液炒熟盛出"},
        {"order": 4, "description": "锅中放油，加入青椒翻炒至断生"},
        {"order": 5, "description": "倒入鸡蛋，加盐调味，翻炒均匀即可"}
      ],
      "cookTime": 10,
      "difficulty": "简单",
      "calories": 280,
      "tags": ["AI 生成", "快手菜"]
    }
  }
    "message": "success"
}
```

---

### 4.3 获取菜谱详情
```
GET /api/recipe/{id}
```

**响应**:

```json
{
  "code": 200,
  "data": {
    "id": 1001,
    "name": "西红柿炒鸡蛋",
    "cover": "https://...",
    "images": ["https://...", "https://..."],
    "author": {
      "id": 10001,
      "nickname": "美食达人",
      "avatar": "https://..."
    },
    "ingredients": [
      {"name": "鸡蛋", "quantity": 3, "unit": "个"},
      {"name": "西红柿", "quantity": 2, "unit": "个"},
      {"name": "盐", "quantity": 3, "unit": "克"},
      {"name": "糖", "quantity": 5, "unit": "克"}
    ],
    "steps": [
      {"order": 1, "description": "鸡蛋打散，加少许盐"},
      {"order": 2, "description": "西红柿洗净切块"},
      {"order": 3, "description": "热锅凉油炒鸡蛋盛出"},
      {"order": 4, "description": "炒西红柿出汁，加糖调味"},
      {"order": 5, "description": "倒入鸡蛋翻炒均匀"}
    ],
    "tips": "鸡蛋打散时加少许水，炒出来更嫩",
    "cookTime": 15,
    "difficulty": "简单",
    "calories": 320,
    "tags": ["家常菜", "快手菜"],
    "stats": {
      "likes": 1234,
      "collections": 567,
      "comments": 89,
      "views": 5000
    },
    "isLiked": false,
    "isCollected": false
  }
}
```

---

### 4.4 菜谱分类列表
```
GET /api/recipe/list
```

**请求参数**:

```
?category=fast&difficulty=simple&page=1&size=20
```

| 参数 | 说明 |
|------|------|
| category | 分类：fast(快手菜)、lowcal(减脂餐)、home(家常菜)、special(特色菜)、health(养生) |
| difficulty | 难度：simple(简单)、medium(中等)、hard(困难) |
| taste | 口味：light(清淡)、spicy(辣)、sweet(甜) |
| keyword | 搜索关键词 |
| page | 页码 |
| size | 每页数量 |

**响应**:

```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "西红柿炒鸡蛋",
        "cover": "https://...",
        "cookTime": 15,
        "difficulty": "简单",
        "calories": 320,
        "tags": ["家常菜", "快手菜"],
        "likeCount": 1234
      }
    ],
    "total": 100,
    "page": 1,
    "size": 20,
    "hasMore": true
  }
}
```

---

### 4.5 今日推荐
```
GET /api/v1/recipe/recommend
```

**Headers**: `Authorization: Bearer <token>`

**响应**:
```json
{
  "code": 200,
  "data": {
    "title": "今天吃什么？",
    "recipes": [
      {
        "id": 1001,
        "name": "西红柿炒鸡蛋",
        "cover": "https://...",
        "reason": "根据你的口味偏好推荐"
      },
      {
        "id": 1002,
        "name": "清炒时蔬",
        "cover": "https://...",
        "reason": "低卡健康，适合减脂"
      }
    ]
  }
}
```

---

### 4.6 搜索菜谱
```
GET /api/v1/recipe/search
```

**请求参数**:

```
?keyword=鸡蛋&page=1&size=20
```

**响应**:

```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "西红柿炒鸡蛋",
        "cover": "https://...",
        "cookTime": 15,
        "difficulty": "简单",
        "calories": 320,
        "tags": ["家常菜", "快手菜"],
        "likeCount": 1234
      }
    ],
    "total": 100,
    "page": 1,
    "size": 20,
    "hasMore": true
  }
    "message": "success"
}
```

---

### 

---

## 五、食材管理模块

### 5.1 获取食材列表
```
GET /api/ingredient/list
```

**Headers**: `Authorization: Bearer <token>`

**请求参数**:
```
?sortBy=expireDate&sort=asc
```

| 参数 | 说明 |
|------|------|
| sortBy | 排序字段：expireDate(保质期)、createTime(添加时间) |
| sort | 排序方式：asc(升序)、desc(降序) |
| status | 状态：all(全部)、normal(正常)、expiring(临期)、expired(过期) |

**响应**:
```json
{
  "code": 200,
  "data": {
    "ingredients": [
      {
        "id": 501,
        "name": "鸡蛋",
        "quantity": 6,
        "unit": "个",
        "category": "蛋类",
        "storageCondition": "冷藏",
        "expireDate": "2026-04-15",
        "daysLeft": 14,
        "status": "normal",
        "createdAt": "2026-04-01T10:00:00Z"
      },
      {
        "id": 502,
        "name": "牛奶",
        "quantity": 1,
        "unit": "盒",
        "category": "乳制品",
        "storageCondition": "冷藏",
        "expireDate": "2026-04-03",
        "daysLeft": 2,
        "status": "expiring",
        "createdAt": "2026-03-28T10:00:00Z"
      }
    ],
    "summary": {
      "totalCount": 10,
      "expiringCount": 1,
      "expiredCount": 0
    }
  }
}
```

---

### 5.2 添加食材
```
POST /api/v1/ingredient
```

**请求参数**:
```json
{
  "name": "西红柿",
  "quantity": 5,
  "unit": "个",
  "category": "蔬菜",
  "expireDate": "2026-04-10",
  "storageCondition": "冷藏"
}
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 503
  }
}
```

---

### 5.3 更新食材
```
PUT /api/v1/ingredient/{id}
```

**请求参数**:
```json
{
  "quantity": 3,
  "expireDate": "2026-04-12"
}
```

---

### 5.4 删除食材
```
DELETE /api/v1/ingredient/{id}
```

---

### 5.5 批量添加食材（快捷录入）
```
POST /api/v1/ingredient/batch
```

**请求参数**:
```json
{
  "ingredients": [
    {"name": "鸡蛋", "quantity": 10, "unit": "个", "expireDate": "2026-04-15"},
    {"name": "牛奶", "quantity": 2, "unit": "盒", "expireDate": "2026-04-10"}
  ]
}
```

---

### 5.6 调味品管理
```
POST /api/v1/condiment
GET /api/v1/condiment/list
PUT /api/v1/condiment/{id}
DELETE /api/v1/condiment/{id}
```

**请求/响应格式同食材管理**

---

### 5.7 临期提醒
```
GET /api/v1/ingredient/expiring-notice
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "expiring": [
      {
        "id": 502,
        "name": "牛奶",
        "expireDate": "2026-04-03",
        "daysLeft": 2
      }
    ],
    "expired": [],
    "summary": {
      "expiringCount": 1,
      "expiredCount": 0
    }
  }
}
```

---

### 5.8 常用食材列表（快速选择）
```
GET /api/v1/ingredient/common
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "categories": [
      {
        "name": "蔬菜",
        "items": [
          {"name": "西红柿", "defaultUnit": "个"},
          {"name": "鸡蛋", "defaultUnit": "个"},
          {"name": "青椒", "defaultUnit": "个"}
        ]
      },
      {
        "name": "肉类",
        "items": [
          {"name": "猪肉", "defaultUnit": "克"},
          {"name": "牛肉", "defaultUnit": "克"}
        ]
      }
    ]
  }
}
```

---

## 六、社区模块

### 6.1 发布菜谱
```
POST /api/community/recipe
```

**Headers**: `Authorization: Bearer <token>`

**请求类型**: `multipart/form-data`

**请求参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| name | string | 菜谱名称 |
| cover | file | 封面图片 |
| images | file[] | 步骤图片（可选） |
| ingredients | string | JSON 数组，食材列表 |
| steps | string | JSON 数组，步骤列表 |
| tags | string | JSON 数组，标签 |
| category | string | 分类 |
| tips | string | 小贴士（可选） |

**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 2001
  }
}
```

---

### 6.2 菜谱列表
```
GET /api/community/recipe/list
```

**请求参数**:
```
?category=home&page=1&size=20
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 2001,
        "name": "我的秘制红烧肉",
        "cover": "https://...",
        "author": {
          "id": 10001,
          "nickname": "美食达人",
          "avatar": "https://..."
        },
        "cookTime": 45,
        "difficulty": "中等",
        "calories": 580,
        "tags": ["家常菜", "特色菜"],
        "stats": {
          "likes": 520,
          "collections": 180,
          "comments": 45,
          "views": 3000
        },
        "createdAt": "2026-03-28T10:00:00Z"
      }
    ],
    "total": 100,
    "page": 1,
    "size": 20,
    "hasMore": true
  }
}
```

---

### 6.3 点赞
```
POST /api/v1/community/recipe/{id}/like
DELETE /api/v1/community/recipe/{id}/like
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "isLiked": true,
    "likeCount": 521
  }
}
```

---

### 6.4 收藏
```
POST /api/v1/community/recipe/{id}/collect
DELETE /api/v1/community/recipe/{id}/collect
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "isCollected": true,
    "collectionCount": 181
  }
}
```

---

### 6.5 评论列表
```
GET /api/v1/community/recipe/{id}/comment
```

**请求参数**:
```
?page=1&size=20
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "comments": [
      {
        "id": 301,
        "user": {
          "id": 10002,
          "nickname": "用户 A",
          "avatar": "https://..."
        },
        "content": "很好的菜谱，收藏了！",
        "likeCount": 10,
        "isLiked": false,
        "createdAt": "2026-03-29T10:00:00Z",
        "replies": [
          {
            "id": 302,
            "user": {
              "id": 10001,
              "nickname": "美食达人",
              "avatar": "https://..."
            },
            "content": "谢谢支持！",
            "createdAt": "2026-03-29T11:00:00Z"
          }
        ]
      }
    ],
    "total": 45,
    "hasMore": true
  }
}
```

---

### 6.6 发表评论
```
POST /api/community/recipe/{id}/comment
```

**请求参数**:
```json
{
  "content": "很好的菜谱，收藏了！",
  "parentId": null
}
```

---

### 6.7 删除评论
```
DELETE /api/v1/community/comment/{id}
```

---

### 6.8 评论点赞
```
POST /api/v1/community/comment/{id}/like
DELETE /api/v1/community/comment/{id}/like
```

---

## 七、个人中心模块

### 7.1 我的收藏
```
GET /api/v1/user/collections
```

**请求参数**:
```
?page=1&size=20
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "西红柿炒鸡蛋",
        "cover": "https://...",
        "author": {
          "nickname": "美食达人"
        },
        "collectedAt": "2026-03-28T10:30:00Z"
      }
    ],
    "total": 15,
    "hasMore": true
  }
}
```

---

### 7.2 我发布的
```
GET /api/v1/user/published
```

**响应格式同收藏列表**

---

### 7.3 历史记录
```
GET /api/v1/user/history
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1001,
        "name": "西红柿炒鸡蛋",
        "cover": "https://...",
        "viewedAt": "2026-04-01T09:00:00Z"
      }
    ],
    "total": 50,
    "hasMore": true
  }
}
```

---

### 7.4 清除历史记录
```
DELETE /api/v1/user/history
```

---

### 7.5 删除发布菜谱
```
DELETE /api/v1/user/recipe/{id}
```

---

### 7.6 编辑发布菜谱
```
PUT /api/v1/user/recipe/{id}
```

---

### 7.7 我点赞的
```
GET /api/v1/user/likes
```

---

## 八、系统模块

### 8.1 帮助中心
```
GET /api/v1/help
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "categories": [
      {
        "name": "账号问题",
        "items": [
          {
            "id": 1,
            "question": "如何登录？",
            "answer": "您可以通过微信一键登录..."
          }
        ]
      },
      {
        "name": "功能使用",
        "items": [
          {
            "id": 2,
            "question": "如何添加食材？",
            "answer": "在食材管理页面点击 + 号..."
          }
        ]
      }
    ]
  }
}
```

---

### 8.2 提交反馈
```
POST /api/v1/feedback
```

**请求参数**:
```json
{
  "type": "bug",
  "content": "菜谱推荐功能出现异常...",
  "contact": "user@example.com",
  "images": ["https://..."]
}
```

| type | 说明 |
|------|------|
| bug | 功能故障 |
| suggestion | 功能建议 |
| other | 其他 |

---

### 8.3 检查更新
```
GET /api/v1/system/check-update
```

**响应**:
```json
{
  "code": 200,
  "data": {
    "hasUpdate": true,
    "version": "1.1.0",
    "title": "版本更新",
    "content": "1. 优化 AI 生成菜谱功能\n2. 修复已知问题",
    "forceUpdate": false,
    "downloadUrl": "https://..."
  }
}
```

---

### 8.4 上传日志
```
POST /api/v1/system/log
```

**请求参数**:
```json
{
  "level": "error",
  "message": "错误信息",
  "extra": {}
}
```

---

## 九、文件上传

### 9.1 上传图片
```
POST /api/v1/upload/image
```

**请求类型**: `multipart/form-data`

**请求参数**:
| 参数 | 类型 | 说明 |
|------|------|------|
| file | file | 图片文件 |
| type | string | 类型：avatar(头像)、background(背景图)、recipe(菜谱)、feedback(反馈) |

**响应**:
```json
{
  "code": 200,
  "data": {
    "url": "https://...",
    "thumbnailUrl": "https://..._thumb"
  }
}
```

---

## 十、接口优先级汇总

| 优先级 | 模块 | 接口 |
|--------|------|------|
| **P0** | 微信登录 | 微信一键登录、获取手机号 |
| **P0** | 菜谱推荐 | 食材输入、AI 生成菜谱、菜谱详情 |
| **P0** | 食材管理 | 食材 CRUD、食材列表 |
| **P0** | 用户信息 | 获取/更新用户信息 |
| **P1** | 个人中心 | 收藏、发布、历史记录 |
| **P1** | 社区 | 发布菜谱、点赞、收藏、评论 |
| **P1** | 设置 | 口味偏好设置 |
| **P2** | 食材管理 | 临期提醒、批量添加、常用食材 |
| **P2** | 菜谱 | 分类筛选、搜索、今日推荐 |
| **P3** | 系统 | 帮助中心、反馈、检查更新 |

---

## 十一、前端微信小程序调用示例

### 登录流程
```javascript
// 1. 微信登录获取 code
wx.login({
  success: (res) => {
    const code = res.code;
    
    // 2. 调用后端登录接口
    wx.request({
      url: 'https://api.example.com/api/v1/auth/wechat/login',
      method: 'POST',
      data: { code },
      success: (res) => {
        const { token, user } = res.data.data;
        // 3. 存储 token
        wx.setStorageSync('token', token);
        wx.setStorageSync('userInfo', user);
      }
    });
  }
});
```

### 获取手机号
```javascript
// 绑定手机号按钮
getPhoneNumber: (e) => {
  if (e.detail.code) {
    wx.request({
      url: 'https://api.example.com/api/v1/auth/wechat/phone',
      method: 'POST',
      data: { code: e.detail.code },
      success: (res) => {
        console.log('手机号:', res.data.data.phoneNumber);
      }
    });
  }
}
```

### 带 Token 的请求
```javascript
// 封装请求
const request = (options) => {
  const token = wx.getStorageSync('token');
  return wx.request({
    ...options,
    header: {
      ...options.header,
      'Authorization': `Bearer ${token}`
    }
  });
};

// 使用
request({
  url: 'https://api.example.com/api/v1/user/profile',
  method: 'GET',
  success: (res) => {
    console.log('用户信息:', res.data.data);
  }
});
```

---

## 十二、数据库设计（核心表）

### 用户表 (user)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| openid | varchar(64) | 微信 openid |
| unionid | varchar(64) | 微信 unionid |
| nickname | varchar(64) | 昵称 |
| avatar | varchar(255) | 头像 |
| background | varchar(255) | 背景图 |
| signature | varchar(128) | 签名 |
| gender | tinyint | 性别 |
| phone | varchar(20) | 手机号 |
| preferences | json | 偏好设置 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 菜谱表 (recipe)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar(128) | 菜谱名称 |
| cover | varchar(255) | 封面图 |
| images | json | 图片列表 |
| author_id | bigint | 作者 ID |
| ingredients | json | 食材列表 |
| steps | json | 步骤列表 |
| tips | text | 小贴士 |
| cook_time | int | 烹饪时间 (分钟) |
| difficulty | tinyint | 难度 |
| calories | int | 卡路里 |
| tags | json | 标签 |
| category | varchar(32) | 分类 |
| source | tinyint | 来源：1-系统 2-用户 3-AI |
| like_count | int | 点赞数 |
| collection_count | int | 收藏数 |
| comment_count | int | 评论数 |
| view_count | int | 浏览数 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 食材表 (ingredient)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| name | varchar(64) | 食材名称 |
| quantity | decimal | 数量 |
| unit | varchar(16) | 单位 |
| category | varchar(32) | 分类 |
| storage_condition | varchar(32) | 存储条件 |
| expire_date | date | 保质期 |
| status | tinyint | 状态 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 收藏表 (collection)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| recipe_id | bigint | 菜谱 ID |
| created_at | datetime | 创建时间 |

### 点赞表 (like)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| target_type | tinyint | 类型：1-菜谱 2-评论 |
| target_id | bigint | 目标 ID |
| created_at | datetime | 创建时间 |

### 评论表 (comment)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| user_id | bigint | 用户 ID |
| recipe_id | bigint | 菜谱 ID |
| parent_id | bigint | 父评论 ID |
| content | text | 内容 |
| like_count | int | 点赞数 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |
