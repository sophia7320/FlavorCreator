/* pages/agreement/agreement.wxss */
.agreement-page {
	--bg-color: var(--bg-primary);
	--card-radius: var(--radius-xxl);
	--title-font-size: var(--font-size-lg);
	--content-font-size: var(--font-size-lg);
	--title-font-weight: var(--font-weight-normal);
	--content-font-weight: var(--font-weight-normal);
}

.agreement-page {
	width: 750rpx;
	height: 100vh;
	background-image: url(https://miniprogram-img-1422554268.cos.ap-guangzhou.myqcloud.com/bg.png);
	background-size: 100% 100%;
	
	display: flex;
	justify-content: center;
	align-items: center;
}

.agreement-container {
	width: 580rpx;
	height: 969rpx;
	padding: 24rpx;

	background-color: var(--bg-color);
	border-radius: var(--card-radius);
}

.content {
	white-space: pre-wrap;
}

.title, .content{
	font-size: var(--title-font-size);
	font-weight: var(--title-font-weight);
}
