.back-btn {
  width: 62rpx;
  height: 62rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 50%;
  box-shadow: var(--shadow-sm);
}

.back-icon {
  width: 29rpx;
  height: 26rpx;
}
